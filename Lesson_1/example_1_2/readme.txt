1.2. QTimer, сигналы и слоты.

Пример описывает создание таймера, и подключение его к обновлению прорисовки объектов.

В данном примере, каждые 33 милисекунды, будет вызыватся метод updateGL(), который в свою очередь, вызывает метод paintGL() и сам отвечает за все переключение буфферов.
